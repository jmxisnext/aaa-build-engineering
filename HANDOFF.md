# Handoff - aaa-build-engineering

## Resume from
Branch: main   |   Last commit: b4eceb0 - track 2 part 5 — REST API bootstrap of the 4-stage build chain

## What was just built
- (no new commits this session — opened, oriented, closed out clean)
- b4eceb0 — **Track 2 part 5 closes the chain.** `ci/scripts/bootstrap-builds.ps1` drives the TeamCity REST API to materialize four build configs under `AAASandbox` attached to the existing `Game Main Stream` VCS root: **Compile → (Smoke Test ∥ Cook Data) → Package** (DAG, not a strict line). Compile runs `cmake -B build && cmake --build`, publishes `build/` as `build.zip`. Smoke Test runs `ctest`. Cook Data runs `hoops_cooker Data Cooked.pak`, publishes the pak. Package runs `cmake --install` + `cp` + `tar`, snapshot-deps on Smoke Test + Cook Data, pulls Compile's build.zip + Cook Data's pak, publishes `hoops-brawl.tar.gz`. Idempotent (skip-if-exists), `-Recreate` to wipe + redo, auto-scrapes the superuser token from `teamcity-server.log` if not given. Verified end-to-end by triggering Package via REST → TeamCity backfilled the chain, all four SUCCESS in ~35s, final `hoops-brawl.tar.gz` (16 KB) contains `dist/bin/{hoops_brawl,hoops_cooker}` + `dist/Cooked.pak` + raw `Data/*.txt`. Lesson #4 banked: TeamCity REST does per-endpoint content negotiation — PUT `/settings/artifactRules` returns `text/plain` and 406s on `Accept: application/json`. Helper now takes per-call Accept.
- 59707b5 — **Track 2 part 4: C++ toolchain in agent image.** Extended `ci/agent/Dockerfile` with `apt-get install --no-install-recommends build-essential cmake` (g++ 13.3.0, cmake 3.28.3, GNU make 4.3 ride along). Toolchain layered ahead of the p4 install so p4 version bumps don't invalidate the heavy layer. Unpinned toolchain version — acceptable for a sandbox, would be lockfile-pinned in production.
- 9aedce2 — **Track 2 part 3: seed `//game/main` with the Hoops Brawl CMake project (CL 24).** 12 files: 10 adds + 2 edits, broker bypassed via `P4PORT=:1666`. Four targets: `hoops_core` (static lib), `hoops_brawl` (game exe), `hoops_tests` (dependency-free unit runner), `hoops_cooker` (asset packer). Shotmeter refactored from inline-in-cpp to header + impl. `ci/samples/hoops-brawl-seed/stream/` is the git snapshot mirror; live source-of-truth is Perforce. Lesson #3 banked: broker is a connection-time router, not a journal — bypassed writes land in the depot but leave no broker-side audit trail. Right long-term fix is a service-account allowlist in broker config; bypass should be a known, *logged-elsewhere* operator action, not the default.
- bfc098e — **Track 2 part 2: custom agent image with p4 baked in.** `ci/agent/Dockerfile` pulls `p4` from `filehost.perforce.com/r25.2/bin.linux26x86_64/` (pinned to broker family). `USER root` for the install (upstream image runs as `buildagent` uid 1000), back to `buildagent` after. `p4 -V` at build time so a bad URL fails the image build, not the first sync. Compose agent service flipped from `image:` to `build: ./agent` with `TEAMCITY_VERSION` threaded as a build arg so server + agent stay on the same release. Broker log confirms `buildagent@<container>` shows up over `LINUX26X86_64` and PASSes — full agent→broker→p4d wire green.

## Live edge
Track 2 is functionally complete. The four-stage CI chain takes a fresh TeamCity server, a custom agent image, a seeded depot, and a bootstrap script — and produces a packaged game build artifact. Nothing is blocking next-step work; the next session has a menu, not a forced path. Top candidate is adding a second build agent so the DAG's Smoke Test ∥ Cook Data fork actually parallelizes (in the verification run they serialized because there's only one agent). Second candidate is the Track 1 broker hardening — adding a service-account allowlist so future infra submits go through the broker instead of bypassing it (closes the audit-trail gap from lesson #3).

## Next
1. **Restart infra.** Start order matters: `perforce\scripts\start-p4d.ps1`, then `perforce\broker\start-broker.ps1`, verify with `p4 -p localhost:1667 info`. Then start Docker Desktop and `docker compose up -d` from `ci/`. Server hits healthy in ~30s, agent re-registers via persisted `data/teamcity_agent/conf/`.
2. **Re-bootstrap if needed.** TeamCity data is persisted via the `data/teamcity_server/datadir` volume so the four build configs survive a restart. If they don't (fresh data dir, server reset, etc.) just run `ci\scripts\bootstrap-builds.ps1` — idempotent, auto-grabs the current superuser token. `-Recreate` if you want a clean wipe.
3. **Pick a follow-up.** Three good options:
   - **Add a second agent** (`docker-compose.yml`: add `teamcity-agent-02` service from the same custom image with a unique `AGENT_NAME`). Confirms the DAG parallelism with a real two-agent run — Smoke Test and Cook Data should fire concurrently. ~30 min of work and a great interview demo (capacity scaling is table stakes).
   - **Broker service-account allowlist** (Track 1 hardening). Modify `perforce/broker/broker.conf` to add a `command: submit { user = build-svc, infra-svc; action = pass; }` rule ahead of the deny-all rule. Then re-do the seed flow as `build-svc` (creating that user, running a submit) and confirm `broker.log` records the submit as `[PASS]`. Closes the audit-trail gap from lesson #3.
   - **Trigger-on-VCS-change.** Add a VCS trigger to the Compile build config so a `p4 submit` to `//game/main` kicks off the chain automatically. REST endpoint is `POST /buildTypes/{id}/triggers`. Trivial addition to the bootstrap script.
4. **(Optional) Kotlin DSL versioned settings.** The REST bootstrap script is the practical answer. The *idiomatic* TeamCity answer is `.teamcity/settings.kts` versioned-settings flow, committed alongside the game code, with TeamCity auto-materializing the build configs from the DSL. Higher interview-narrative value but also higher complexity (DSL syntax for 2026.1, Maven fetch on the server, UI-side enable step). Defer until either (a) you want the polish, or (b) the REST script becomes painful to maintain.
</content>
</invoke>
# Pin TeamCity image version here once a known-good is identified.
# `latest` is fine for the sandbox bring-up; pin before checking in any
# build configs so future runs are reproducible.
TEAMCITY_VERSION=latest

# Python virtual environments
**/.venv/

# Extracted P4Python wheels (regeneratable via tools/README.md setup steps)
**/_p4python_wheels/

# The vendor-distributed P4Python zip (re-downloadable from Perforce)
**/p4python-*-x64-whl.zip

# Python bytecode
**/__pycache__/
*.py[cod]

# C++ build artifacts (accel/ samples compile here)
**/_build/
*.obj
*.exe
*.pdb
*.ilk

# FASTBuild: vendor binaries (re-download via accel/tools/fastbuild/README.md)
# + machine-specific generated config + dependency DB
accel/tools/fastbuild/FBuild*.exe
accel/tools/fastbuild/LICENSE.TXT
**/_generated.bff
*.fdb

# Vendored bgfx/bx/bimg source trees (Track 3 workload tier).
# Re-clone via accel/scripts/setup-bgfx.ps1; pinned SHAs in vendored.lock.json.
accel/extern/

# Track 4 dashboard: bench -Json emits the collector reads (re-generatable)
accel/.metrics/

# Track 4 (unreal): UBT build logs + metric emits (re-generatable; logs are large)
unreal/.logs/
unreal/.metrics/

# Dashboard preview output (throwaway; the committed artifact is dashboard.html)
dashboard/_preview.html

# Environment files — never commit real secrets. ci/.env is intentionally tracked
# (contains only TEAMCITY_VERSION); re-include it explicitly.
**/.env
!ci/.env

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
.vscode/
.idea/

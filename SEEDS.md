# Seeds - aaa-build-engineering

Parked ideas and associative jumps. Edited as needed.
Closeout may append here but never overwrites this file.

- [resolved 2026-05-15] ~~Repo name `unreal-unity-cpp` is now mildly misleading — consider renaming before any inbound links exist.~~ → Renamed to `aaa-build-engineering` on 2026-05-15 before Track 1 started.
- [planted 2026-05-15] If track 4 turns into something substantial, Horde is the strongest signal for a Build Engineer interview — it's the modern Epic-blessed CI and increasingly the industry default for Unreal shops. Worth pushing further than the roadmap suggests.
- [planted 2026-05-15] An interview-worthy mini-project across all 5 tracks: build a small but real "AAA-shaped" pipeline. Real P4, real CI, real cook, real package, real version stamp, packaged & runnable. Frame the whole repo as the demo.
- [planted 2026-05-15] `perforce/lessons-learned.md` is by far the highest-leverage artifact in the repo for interview prep — each gotcha doubles as a "tell me about a time" answer. Pattern worth replicating: every track should produce a similarly structured `lessons-learned.md` (numbered incidents → why a build engineer cares → interview-ready TL;DR bullets). Don't make this an afterthought; capture lessons as they happen, not at the end of a track.
- [planted 2026-05-15] The "custom agent Dockerfile installs missing tool X" pattern is much more general than the p4-binary case from Track 2 lesson #2. The same recipe — `FROM jetbrains/teamcity-agent:latest` + RUN-install-tool — is how you'd add MSVC build tools, FASTBuild, Incredibuild agent, Blender for content cook, Houdini, etc. to an agent image. Track 2 step 3 (sample C++ project) and Track 3 (build acceleration / FASTBuild) both want this pattern. Worth a small `ci/agent/` subdir holding one Dockerfile that grows over time, with each tool-add accompanied by a one-line note in the agent README explaining *why* that tool is there. That doc-as-you-go discipline is the only thing that keeps an agent image from rotting into "we installed X in 2023 and nobody remembers why."
- [planted 2026-06-03] The `accel/scripts/demo-mp.ps1` pattern — generate N heavy TUs, compile two ways, best-of-3 cold reps, report a speedup table — is a reusable *build-acceleration measurement harness*, not a one-off. Every remaining Track 3 lever is the same harness with one variable changed: unity/jumbo = concatenate the N TUs into one before compiling; PCH = `/Yc` once + `/Yu` per TU vs no-PCH; FASTBuild = the fbuild orchestrator vs raw cl. Worth factoring the timing/reporting core out of demo-mp into a shared helper so each new lever is ~20 lines + a README row. The interview value compounds: one consistent before/after table across /MP, unity, PCH, FASTBuild tells a complete "how I'd cut a 25-min build to 12" story with real numbers, not anecdotes.
- [planted 2026-06-03] The agent-pool benchmark (`bench-agents.ps1`) shows a clean 2x, but on this toy project every build is ~11s of *fixed overhead* (p4 sync + artifact download + handshake) - the 2x is overhead-overlap, not compute-parallelism. A stronger interview demo would give the leaf stage real compute (a longer test suite, or a Cook Data step that cooks a non-trivial asset set) so the 2x reflects work being done in parallel, not just I/O waiting in parallel. Same harness, heavier leaves. Pairs with the Track 3 acceleration-measurement-harness seed above.
- [planted 2026-06-03] **Pre-flight / gated builds = the next Track 2 lever after the VCS trigger.** The VCS-trigger lever (post-commit CI) catches breakage *after* it lands on `//game/main`. The industry-leading model validates *before* submit: TeamCity's **Perforce Shelve Trigger** runs **personal builds** on shelved changelists (keyword-matched), optionally gated through **P4 Code Review/Swarm**, so mainline stays green by construction. Deliberately scoped out of the 2026-06-03 VCS-trigger spec (`docs/superpowers/specs/2026-06-03-vcs-trigger-design.md` §8) because it's a meaningfully bigger build (shelve workflow + personal builds + ideally a review gate). The arc: post-commit was the floor; pre-flight is moving up to "broken changes never reach the mainline."
- [planted 2026-06-04] The last two unscripted reset steps - TeamCity **agent authorization** and the **first-run setup wizard** (admin account + maintenance token) - are what keep a *full datadir wipe* from being hands-off (the config-level reset already is). Agent auth looks scriptable via REST (`PUT /app/rest/agents/<locator>/authorizedInfo`); the wizard is harder (maintenance-token flow, possibly a seeded `teamcity-server-config` mount). Scripting these would make even a from-empty-server reset fully automated. Surfaced from the honest-reset analysis behind lesson #9.
- [planted 2026-06-04] **Workload tier:** the portfolio's numbers stay toy/overhead-bound until the heavy-lifting tracks run against a REAL, recognizable workload. Adopt one, do NOT build a game from scratch (target = build-engineering, not gameplay). bgfx `examples/common` for Track 3 (raw C++ accel); Lyra for Tracks 4–5 (UE compile/cook/package) - now fully viable on the RTX 3060 12 GB. CitySample = heavier stress option, RAM-tight at 31 GB. See ROADMAP_NEXT.md.
- [planted 2026-06-04] `bench-agents.ps1` very likely has the SAME latent CSRF-on-writes bug just fixed in bootstrap-builds.ps1 + setup-vcs-trigger.ps1 (lesson #10): it enables/disables agent-02 via REST PUT (`/agents/.../enabledInfo`), a write that TeamCity 2026.x rejects without an X-TC-CSRF-Token. It will fail on the next bench run against a 2026.x server. Patch it the same way (web session + per-session CSRF token on writes) BEFORE relying on the next agent-pool benchmark - and consider factoring the CSRF-aware Invoke-TC into one shared ci/scripts helper so a fourth REST script cannot reintroduce it.
- [planted 2026-06-04] CI sandbox pins `TEAMCITY_VERSION:-latest` in `ci/docker-compose.yml`, so the server version silently drifts on each `docker compose pull` (currently 2026.1 / build 222521; 2026.1.1 / 222577 shipped 2026-05-29 with 2 security fixes + a Perforce changelist-description fix TW-100761). Pinning an explicit build would make the infra reproducible and stop the drift — matches the repo's reproducibility ethos and the server+agent same-release note. Surfaced from the 2026.1.1 release-notes check; deferred FYI-no-action.
- [planted 2026-06-04] The four Track-4 rung wrappers (`compile-lyra.ps1`, `cook-lyra.ps1`, `package-lyra.ps1`, `buildgraph-lyra.ps1`) now share a large copy-pasted spine: beyond the engine/uproject discovery already factored into `_unreal-common.ps1`, every one repeats the **stopwatch → `& <tool> @args 2>&1 | Tee-Object <log>` → `$LASTEXITCODE` capture → write metric-JSON to `.metrics`** block (~30 near-identical lines ×4). Factor that timing/log/metric core into one `Invoke-TimedBuildStep` helper in `_unreal-common.ps1` (args: step name + tool path + arg array + extra metric fields; returns/writes the metric object), so each wrapper collapses to discovery + arg-assembly + one call. Mirrors the `accel/` "factor the timing/reporting core out of demo-mp" seed above (line 11) — same DRY pattern, different track. Worth doing **before** rung #5's TeamCity/version-stamp wrapper copies the spine a fifth time. Also unifies the metric schema across steps (handy for the rung #6 dashboard ingest).
- [planted 2026-06-04] UBT warns MSVC 14.44 is "not a preferred version" (UE 5.6 prefers 14.38). Harmless so far - the slice-#1 failure was commit-limit, not toolchain, and 14.44 linked LyraEditor fine. But a too-new MSVC can cause engine-code compile errors in some UE builds. If a later/heavier Lyra compile (cook, or a Shipping/clean build) hits odd C++ errors, install the UE5.6-preferred 14.38 toolset (VS Installer -> Individual components -> "MSVC v143 - VS2022 C++ x64/x86 build tools (v14.38)") and let UBT pick it, before chasing the code.
- [planted 2026-06-04] Put Lyra content in its own P4 depot/stream so the rung-#5 stamp threads a REAL Lyra content changelist (true-to-studio), instead of standing in the sample //game/main CL via %build.vcs.number%. Would make the CL-stamped-package demo end-to-end authentic.
- [planted 2026-06-04] Script the native Windows TeamCity agent bring-up into ci/scripts/setup-win-agent.ps1 (download agent zip, stage JRE into \jre, verify/point pwsh Core, write buildAgent.properties, start, authorize via CSRF-safe REST) - currently all manual; lessons #14.
- [graduated → .jam/SEEDS.md 2026-06-16] [planted 2026-06-04] jam:closeout/startup blind spot: two handoffs framed the dashboard (rung #6) as not-yet-built while it was already committed (buried below git log -5). Consider having closeout/startup cross-check the handoff against actual committed state (e.g. git log --oneline of feat commits, or a feature ledger), not just recent-commit memory. — This is a jam-plugin improvement, not aaa-build track work; moved to the plugin's own backlog. Kept here as provenance.
- [planted 2026-06-05] Optional de-identification: the bare username `james` appears in ~18 tracked files (workspace names like `james-WS01-...`, `$P4User` defaults, `p4broker.conf`). Not a secret and the repo isn't anonymous (it's `jmxisnext`'s portfolio) → low priority, but scrubbing to a generic `devuser`/`dev01` would make the demo scripts read as studio-portable. Surfaced by the 2026-06-05 sanity-check (pubsafe-1); deferred as cosmetic.

- [superseded 2026-06-11 → kernel lives in its OWN repo, the `chronicle-kernel` experiment (now created), NOT as an aaa-build track; see the amendment below. Kept for the spec pointer + provenance.] [planted 2026-06-11] **Chronicle G0 distortion kernel** as a candidate new track (simulation/determinism — the gameplay-AI re-anchor direction): deterministic typed-distortion engine over a synthetic event ledger (~500-800 lines, stdlib, offline tests), two witness-set-driven registers, template chronicle renderer, diff annotator naming the transform behind each truth-vs-record gap, pre-registered culture-vs-noise protocol (consistent-but-arbitrary null, mechanical criteria gating) with KILL semantics. The falsification gate for a larger civilization-memory engine; demoable artifact = run CLI -> chronicle + attributable diff, byte-identical under seed. Spec + full ideation record: J:\jammers-lab\noosphere\docs\ideation\2026-06-11-chronicle-engine\07-chronicle-g0-kernel.md (+ 00-INDEX.md scoreboard). Sequencing vs the declared live edge (Horde+UBA step 2) is an in-session decision - do not displace silently; track README should name this as the simulation track deliberately (ROADMAP_NEXT's "target role is build-engineering" caveat acknowledged).

- [planted 2026-06-11] **Gameplay-skill tracks / game-demo venue** — the re-anchor's full intent (James, 2026-06-11): use this repo to build gameplay/simulation skills and eventually begin demoing the Mythogenic game itself, on the already-paid-for UE5/Lyra/BuildGraph substrate from Track 4. Two-speed rule from the chronicle-engine ideation: (a) SKILL tracks — UE gameplay-systems artifacts (turn-based combat toy, GAS, AI behavior, save/serialization), each self-contained + demoable, honestly named as skill-building — are fair game whenever the roadmap allows; (b) DEMOING THE GAME ITSELF stays gated behind the Chronicle G0 kernel gate — a hero-era slice before the engine demos commodity JRPG mechanics and tests nothing the project bets on (ideation candidate 04, skip). Bridge artifact that satisfies both: post-gate, the "thinnest played consumer" (04 salvage) becomes the first UE gameplay track — a chronicle correction-quest demo consuming real kernel output, cooked + packaged by this repo's own BuildGraph pipeline. That is simultaneously a gameplay-skill artifact, the game's first honest demo, and a build-pipeline workload. See J:\jammers-lab\noosphere\docs\ideation\2026-06-11-chronicle-engine\04-mythogenic-game-first.md + 07-chronicle-g0-kernel.md.

- [amended 2026-06-11, same day] Chronicle home FINAL SHAPE: the G0 kernel lives in its OWN private lab repo (separate experiment under J:\jammers-lab, name TBD e.g. chronicle-kernel) that CONSUMES this repo's infrastructure - TeamCity config (tests-on-commit, SHA version-stamp, failure notifier), determinism gate as a CI job (run twice same seed, byte-diff artifacts), optional dashboard feed (name jobs generically; committed snapshot.json is public), and post-gate the UE/Lyra/BuildGraph pipeline for the correction-quest demo. The earlier "candidate simulation track subdir" idea is superseded: aaa-build publicly carries only the pipeline/integration story, keeping the build-engineering identity pure. Kernel code goes public only if/when the gate passes.
- [planted 2026-06-13] Wire a shared/Horde DDC so the Horde cook reuses the warm cache — this session's Horde cook was ~24.6 min (cold-ish: Vulkan SM6 perms uncached) vs the TeamCity warm path; a shared DDC turns a ~24-min cold cook into ~1-min, the single biggest cook-throughput lever and a strong Horde-mechanics demo (ties to lessons-learned #4).

- [planted 2026-06-14] De-id (Session 1) scrubbed only the ACTIVE demo surface (scripts/configs/user-facing docs); `james` was deliberately LEFT in `docs/superpowers/*` historical plan+spec artifacts (~15 hits, 4 files) and in captured-log lines (ci+perforce lessons-learned, the broker.log sample) as honest provenance. If a "zero james anywhere public" bar is ever wanted, sweep the historical docs too — but note they embed then-current script source AND real verification commands actually run as super `james`, so scrubbing them rewrites a dated record (treat like editing a log, not config).
- [planted 2026-06-17] **Second-engine arm (Unity > Godot), as a POST-Capstone breadth play — not a mid-stream pivot.** Unity is the strong candidate, and specifically because the **Unity Accelerator is a shipping content-addressable cache (a real DDC/CAS)** and Addressables content-build is a cook analog — both map directly onto Track 5's "hand-rolled DDC/CAS" thesis, enabling an "industrial version vs my mini version, here's the delta" story; transferable build surface = `-batchmode` BuildPipeline + Accelerator + Addressables; also broadens past Unreal-only shops. **Godot is weak for THIS portfolio's thesis** — clean/scriptable (`godot --headless --export`) and fine for a cheap open CI/CD showcase, but it has no distributed build, no cook-at-scale, no CAS, no Perforce-at-scale, so it can't *exhibit* solving the AAA-scale problems the whole repo is built around (revisit only if deliberately targeting a broader/indie market). The discipline that outranks the engine choice: current edge is DEPTH on one stack (UBT→UBA→Horde→cook→package→BuildGraph→TeamCity, real numbers); three half-built engine ladders read worse than one deep one — don't fork engines until the Unreal spine is capped by the Capstone (Phase 2 Step 4).
- [planted 2026-06-20] **Track 5 step 3 — C# WPF artist tool over the cooker.** Wrap `pipeline/cook.py` in a WPF/.NET desktop UI (the recognizable AAA artist-tooling stack; ROADMAP_NEXT notes "WPF or Blazor"): pick input folder (→ `--src`), trigger a cook with progress, view output sizes (from `manifest.toc.json`) + the character→asset dependency graph. Interop seam = shell out to `cook.py` as a subprocess and read back the `.toc` + `--stats-json` (the cooker stays the single tested source of truth; WPF is a thin shell). Gaps to close when picked up: dep-graph EDGES aren't serialized into the `.toc` yet (emit them, or re-parse character JSON); "stale entries" view wants a cooker `--dry-run` mode (compute cook-keys, report would-recook, write nothing). Catch: Windows-only GUI + a new .NET toolchain in the repo, not headless-testable — keep all logic in the Python cooker.
- [planted 2026-06-20] **Track 2↔5 — wire the real cooker into CI "Cook Data".** Replace the toy C++ `hoops_cooker` (`ci/.../Tools/Cooker`, concatenates `Data/*.txt` → `Cooked.pak`) in the TeamCity DAG (Compile → {Smoke Test, Cook Data} → Package) with `pipeline/cook.py`, so the CI cook does a REAL content cook (textures/audio/characters, incremental, content-addressed). Smaller / higher-signal than the WPF tool (~½ session) and adds a real cross-track edge. Catches: (1) output-shape mismatch — Package expects a single `Cooked.pak` but the cooker emits a `cooked/` CAS dir + `.toc`; add a "pack cooked blobs+toc → .pak" step to the cooker (keeps the CI contract) or change Package to bundle the dir. (2) agent needs Python+Pillow — one `RUN pip install pillow` in `ci/agent/Dockerfile` (the existing "agent installs missing tool X" pattern). (3) incremental only pays off if `cooked/`+cook-index persist across CI runs (checkout reuse or artifact cache) → a "warm vs cold CI cook" number = a dashboard feed.

- [planted 2026-06-22] Stand up **Zen Server** as its own CAS-backed-DDC mechanics demo — distinct from the local shared-folder DDC (`set-shared-ddc.ps1`, `UE-SharedDataCachePath`) used in the heavy-run prep campaign purely for the cook-warm optimization. Zen is the modern UE DDC paradigm (content-addressable store by hash, default backend since UE 5.4) and a strong interview artifact, but it is a separate heavy demo, not offline prep. Surfaced during the 2026-06-21 prep campaign (spec §6).

- [planted 2026-06-22] **Shared-DDC payoff is a CROSS-MACHINE story, not a two-cook-on-one-box demo.** Live finding (2026-06-22, lesson #5): on a box with a warm LOCAL DDC, an empty SHARED DDC (`UE-SharedDataCachePath`) stays sparse — local hits don't back-propagate, so the shared folder only captured the 36 MB delta and the cook ran ~108s, not a cold ~24 min. So cold→warm via the shared folder is **unobservable on one box** whose local DDC is already warm (a warm cook #2 shows no delta). To actually demo it: TeamCity **native Windows agent ↔ Horde agent** (each with its own local DDC, sharing one D:\ DDC), or wipe the local DDC first so a cook misses local and writes through to shared. Documented cold number stays 24.6 min (2026-06-13). Pairs with the Zen-Server seed above (Zen is the cross-machine CAS done properly).

- [planted 2026-06-22] **Add a repo↔deployed config-drift check to `horde-preflight.ps1`.** The first live Horde run after authoring the in-graph stamp stamped `source=teamcity` because the live server's config (`C:\ProgramData\Epic\Horde\Server\game-main.stream.json`) was a STALE copy missing `-set:Source=horde` — the repo had the fix, the running server didn't (lesson #6). A preflight check that diffs `unreal/horde/config/*.json` against the deployed `C:\ProgramData\Epic\Horde\Server\*.json` and FAILs/WARNs on drift would have caught this offline, before burning a live run. Same de-risk logic the prep campaign is built on (machine-state guardrails), applied to config. Cheap (~½ session); strong "the running system ≠ the repo" interview point.

- [planted 2026-06-24] **HANDOFF → stand up a separate UE6/Verse experiment (research → `/jam:new`); do NOT pivot aaa-build.** Decided this session (2026-06-24): James is **explicitly gearing up for UE6**, so Verse/UE6 work lives in its **own jammers-lab experiment**, not here — keeping aaa-build's build-engineering identity pure (same precedent + reasoning as the chronicle-kernel split, SEEDS 2026-06-11 amendment). Consequence: **Track-5 step-3 tool stays C# WPF / PySide6** — Verse is a gameplay-scripting language, NOT a build tool (confirmed category error; full research brief produced in this session). Run this prompt in a fresh dedicated session:

  > **PROMPT — "Research UE6 + Verse, then scaffold a new jammers-lab experiment via `/jam:new`."**
  >
  > **PHASE 1 — RESEARCH** (verify-don't-assume; cite + date every source — the UE landscape moves fast):
  > 1. **UE6 status & timeline** — Early Access reportedly **late 2027**; announced at **State of Unreal 2026 (2026-06-17)**, where Epic said UE6 merges UE5 + UEFN and positions **Verse as the gameplay-scripting successor to C++/Blueprints** ("over years"). Confirm current status.
  > 2. **How to practice Verse NOW** — as of mid-2026 Verse runs **only inside the Unreal/UEFN runtime** (no standalone/CLI runtime; rides the Blueprint VM). **UEFN (Unreal Editor for Fortnite)** is the only way to run Verse today. Find the current onboarding path.
  > 3. **Verse language model** — functional; `<decides>`/failure contexts; transactional concurrency (`spawn`, structured concurrency); speculative execution; tooling = VS Code extension + LSP (parse/semantic only, no standalone compile) + the UEFN editor. Scope what a beginner builds first.
  > 4. **Demoable win-condition** — the *smallest real* Verse gameplay artifact (one mechanic: a scored interaction / device behavior), honestly framed as "learning the UE6 gameplay-scripting successor." NOT a game from scratch.
  > 5. **Hardware fit** — RTX 3060 12 GB / 31 GB RAM runs UEFN fine; confirm install footprint + drive placement (installs → G:, scratch → D:; see auto-memory `dev-machine-specs`).
  > 6. **Bridge to aaa-build (keep accurate, don't overstate)** — near-term **UEFN publishes through Epic's own cloud pipeline: self-contained, does NOT route through aaa-build's BuildGraph/Horde/cook pipeline.** Only a LATER UE6-proper standalone project could be cooked/packaged by aaa-build's pipeline (the chronicle correction-quest demo pattern, SEEDS 2026-06-11). So: **separate repo now; optional build-substrate bridge later.**
  >
  > **PHASE 2 — SCAFFOLD** — `/jam:new` (suggested name `[redacted]`). CLAUDE.md scope contract: win-condition = a demoable Verse gameplay artifact built in UEFN; **out of scope** = build-engineering (that's aaa-build) and Verse-as-build-tool (category error). Seed HANDOFF/SEEDS/README per the lab convention. Cross-link: aaa-build (build substrate + later bridge) and the **2026-06-11 gameplay-skill-tracks / Mythogenic seeds** — this experiment can be the gameplay-skill vehicle that intent described.
  >
  > **STARTING POINT** — a full Verse-viability research brief was produced in the aaa-build session 2026-06-24 (no standalone runtime; UEFN-only; UE6 EA ~late 2027; **Python** is UE's editor-tooling language; **BuildGraph/Horde/UAT** is the build layer; Rockstar titles tools roles "C# WPF"). Reuse it; re-verify anything time-sensitive.

- [planted 2026-06-24] Force the **Cook Assets** warm-cache round-trip **across two agents** (cold on agent-01 → warm on agent-02 reusing the server-published CAS). The 2026-06-24 live validation proved cold→warm but both builds happened to land on `agent-linux-02`; cross-agent reuse is *architecturally* covered (the CAS is a server-side artifact, not agent-local) but not *empirically* forced — pinning the two builds to different agents would close spec §8's last sub-item and make a stronger "the cache travels with the build, not the agent" demo.
- [planted 2026-06-24] Teach dashboard/collect-metrics.ps1 to pull the Cook (Track 5) feed live from TeamCity (latest CookAssets cook-stats artifact), like the CI feed - removes demo-capstone's manual cook-stats download + the local-file asymmetry. Deferred during Capstone Slice 1.

- [planted 2026-06-24] [spun out → jammers-lab/[redacted]] **Gameplay-systems SAD re-proposed → slim Verse patterns charter.** [redacted] (8 pillars / ~48 docs) sanity-checked (Verse transactional + structured-concurrency verified; persistence constrained; UE6 ~2027+) and slimmed to **3 Verse-distinctive pillars + 1 runnable UEFN reference mechanic** (trait-inheritance transactional commit, mythogenic-seeded). Spun out to `[redacted]`. **Designated [redacted]** (Track 1) — thesis DEFERRED (leading: *[redacted]*, the layer [redacted]/[redacted]/[redacted] lack); incubate lab-side now, promote to [redacted] on G0. Full record: docs/ideation/2026-06-24-gameplay-systems-sad/.
- [planted 2026-06-24] Engine-agnostic gameplay-patterns library (cross-experiment index) — parked: `chronicle-kernel` already IS this (with a tighter gate); an index is doctrine-gardening. Revisit only with a 2nd sim experiment. See docs/ideation/2026-06-24-gameplay-systems-sad/03-engine-agnostic-patterns-library.md
- [planted 2026-06-24] Re-ground `chronicle-kernel` with SAD vocabulary (state-ownership maps / authority tables) — parked/contingent: gilds a repo that already has its protocol; lift at most one ownership table, and only post chronicle G0. See docs/ideation/2026-06-24-gameplay-systems-sad/04-chronicle-kernel-reground.md
- [planted 2026-06-24] Build `mythogenic` in Verse — parked → tandem: right direction, premature now (UEFN-only runtime; UE6 general runtime ~2027+); becomes `[redacted]` G2 target — the reference prototype already seeds a thin slice. See docs/ideation/2026-06-24-gameplay-systems-sad/05-mythogenic-in-verse.md
